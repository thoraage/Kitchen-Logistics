package no.simplicityworks.kitchenlogistics.util

/*import android.database.Cursor

class RichCursor(cursor: Cursor) extends Iterable[Cursor] {
  override def iterator = new Iterator[Cursor] {
    override def hasNext = !cursor.isLast

    override def next() = {
      cursor.moveToNext()
      cursor
    }
  }
}
  */